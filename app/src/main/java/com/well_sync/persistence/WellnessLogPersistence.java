/*package com.well_sync.persistence;

import com.well_sync.objects.UserDetails;
import com.well_sync.objects.MoodLog;
import com.well_sync.objects.SymptomLog;
import com.well_sync.objects.MedicationLog;
import com.well_sync.objects.SubstanceUseLog;

import java.util.*;

public interface WellnessLogPersistence {
    void setMoodLog(UserDetails user, MoodLog moodLog, Date date);

    MoodLog getMoodLog(UserDetails user, Date date);

    void setSymptomLog(UserDetails user, SymptomLog symptomLog, Date date);

    SymptomLog getSymptomLog(UserDetails user, Date date);

    void setMedicationLog(UserDetails user, MedicationLog medicationLog, Date date);

    MedicationLog getMedicationLog(UserDetails user, Date date);

    void setSubstanceUseLog(UserDetails user, SubstanceUseLog substanceUseLog, Date date);

    SubstanceUseLog getSubstanceUseLog(UserDetails user, Date date);
}*/
